<<<<<<< SEARCH
  // Parse thumbnails from model.thumbnail
  // The model.thumbnail should contain all thumbnails separated by ::
  const thumbnailString = model.thumbnail;

  const allThumbnails = thumbnailString ? parseThumbnails(thumbnailString) : [];
  const hasMultipleThumbnails = allThumbnails.length > 1;
  const currentThumbnailIndex = 0; // Start with first thumbnail (default)
  const currentThumbnail = allThumbnails.length > 0 ? allThumbnails[currentThumbnailIndex] : null;
=======
  // When model data is loaded without the actual thumbnail string to save memory,
  // we check the hasThumbnail flag. The client will fetch thumbnails individually.
  const hasThumbnailData = typeof model.thumbnail === 'string';
  const hasAnyThumbnail = hasThumbnailData ? !!model.thumbnail && model.thumbnail !== '3d.png' : !!model.hasThumbnail;

  // Create an image element to display the thumbnail, but defer setting the source
  const img = document.createElement('img');
  img.style.width = thumbSize.width;
  img.style.height = thumbSize.height;

  // We will load the thumbnail asynchronously to not block UI rendering
  if (hasAnyThumbnail) {
    if (hasThumbnailData) {
      const allThumbnails = parseThumbnails(model.thumbnail);
      if (allThumbnails.length > 0) {
        img.src = allThumbnails[0];
      } else {
        img.src = '3d.png';
      }
    } else {
      // Lazy load from backend
      window.electron.getThumbnail(model.filePath).then(thumb => {
        if (thumb && thumb !== '3d.png') {
          img.src = thumb;
        } else {
          img.src = '3d.png';
        }
      }).catch(err => {
        console.error('Failed to load thumbnail lazily', err);
        img.src = '3d.png';
      });
    }
  } else {
    img.src = '3d.png';
  }

  thumbnailContainer.appendChild(img);

  const parseThumbnails = (thumbnailString) => {
    if (!thumbnailString || thumbnailString === '3d.png' || !thumbnailString.includes('::')) {
      return [thumbnailString].filter(t => t && t !== '3d.png' && t.length > 0);
    }
    // Split and filter out invalid entries - only keep valid data URLs
    return thumbnailString.split('::').filter(t => {
      return t && typeof t === 'string' && t.length > 0 && t !== '3d.png' && t.startsWith('data:image');
    });
  };

  // Note: For detailed view with multiple thumbnails, the implementation was relying on model.thumbnail
  // In lazy load mode, we skip the multiple thumbnails navigation for simplicity unless explicitly requested
  let hasMultipleThumbnails = false;
  let thumbnailWrapper = thumbnailContainer;
  let currentThumbnail = img.src;
>>>>>>> REPLACE
